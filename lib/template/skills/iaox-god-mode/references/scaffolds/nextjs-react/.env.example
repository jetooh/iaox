# Secrets desta app (copie para .env e preencha). O .env NÃO é commitado.
# NEXT_PUBLIC_* é exposto ao browser — use só para valores públicos, NUNCA segredos.

# NEXT_PUBLIC_API_URL=
# Exemplos de segredos (server-only, sem prefixo NEXT_PUBLIC_):
# DATABASE_URL=
# API_SECRET_KEY=
