# Secrets desta app (copie para .env e preencha). O .env NÃO é commitado.
# Prefixo VITE_ é exposto ao browser — use só para valores públicos, NUNCA segredos.

# VITE_API_URL=
# Exemplos de segredos (server-only, sem prefixo VITE_):
# DATABASE_URL=
# API_SECRET_KEY=
